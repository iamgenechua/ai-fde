# Required — agent won't work without this
OPENAI_API_KEY=

# Optional — PostHog analytics (falls back to mock data if missing)
POSTHOG_PERSONAL_API_KEY=
POSTHOG_PROJECT_ID=
POSTHOG_HOST=https://us.i.posthog.com
